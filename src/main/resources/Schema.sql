/*CREATE TABLE items
(
    id    serial PRIMARY KEY,
    image varchar(50),
    name  varchar(25),
    color varchar(25),
    price varchar(25)
);


CREATE TABLE users
(
    id       serial PRIMARY KEY,
    name     varchar(25),
    surname  varchar(25),
    email    varchar(255),
    password varchar(25)
);
*/

DROP TABLE users
